/**
 * Hero treatment. "dark" puts the wordmark and nav on a near-black ground;
 * "light" opens on paper with the violet reserved for the accent and CTA.
 * Switch here — the header lockup and button variants follow automatically.
 */
export type HeroVariant = "dark" | "light";

export const HERO_VARIANT: HeroVariant = "light";
